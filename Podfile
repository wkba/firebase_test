# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'firebase_sample' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for firebase_sample
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'

  target 'firebase_sampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firebase_sampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
